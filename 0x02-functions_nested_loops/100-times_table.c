#include <stdio.h>
#include "main.h"
/**
 * print_times_table - print times table of n
 * @n: number to get times table of
 *
 * Return: no return value
 */
void print_times_table(int n)
{
	if (n > 0 && n < 15)
	{
		int m;
		int ans = n * m;
		int ans_next = n * (m + 1);

		for (n = 0; n <= 9; n++)
		{
			for (m = 0; m <= 9; m++)
			{
				
			}
		}
	}
}
