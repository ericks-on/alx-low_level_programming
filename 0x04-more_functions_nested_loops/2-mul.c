#include "main.h"
/**
 * mul - multiplies two integers
 * @a: requires first factor
 * @b: requires second factor
 *
 * Return: returns product
 */
int mul(int a, int b)
{
	int ans;

	ans = a * b;
	return (ans);
}
